{
    "require": {
        "vlucas/phpdotenv": "^5.6",
        "phpmailer/phpmailer": "^7.0"
    },
    "scripts": {
        "audit": "composer audit",
        "audit-format": "composer audit --format=json"
    },
    "config": {
        "audit": {
            "abandoned": "report"
        }
    }
}
